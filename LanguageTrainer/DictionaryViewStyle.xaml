<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Стиль заголовка словаря -->
    <Style x:Key="DictionaryViewTitleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontFamily" Value="Comic Sans MS"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="TextAlignment" Value="Left"/>
        <Setter Property="Margin" Value="20,10,0,0"/>
    </Style>

    <!-- Стиль для контейнера пары слов -->
    <Style x:Key="WordPairContainerStyle" TargetType="Grid">
        <Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="20,5,0,5"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
    </Style>

    <!-- Стиль для TextBox в режиме создания нового слова -->
    <Style x:Key="NewWordTextBoxStyle" TargetType="TextBox">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Comic Sans MS"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#58567D"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="0,0,20,0"/>
        <Setter Property="MinWidth" Value="150"/>
    </Style>

    <!-- Стиль для TextBox в режиме редактирования существующего слова -->
    <Style x:Key="EditWordTextBoxStyle" TargetType="TextBox">
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Comic Sans MS"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#58567D"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5,0"/>
        <Setter Property="MinWidth" Value="50"/>
    </Style>

    <!-- Стиль для разделителя слов -->
    
    <Style x:Key="WordSeparatorStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="Gray"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontFamily" Value="Comic Sans MS"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- Стиль для Border с текстом слова -->
    <Style x:Key="WordBorderStyle" TargetType="Border">
        <Setter Property="BorderBrush" Value="#58567D"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Margin" Value="5,0"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="IBeam"/>
    </Style>

    <!-- Стиль для TextBlock внутри Border слова -->

    <Style x:Key="WordTextBlockStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Comic Sans MS"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="Margin" Value="5,0"/>
        <Setter Property="Cursor" Value="IBeam"/>
        <Setter Property="Padding" Value="0,0,0,2"/>
    </Style>

    <!-- Стиль для кнопки удаления слова -->
    <Style x:Key="DeleteWordButtonStyle" TargetType="Button">
        <Setter Property="Content" Value="×"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
    </Style>

</ResourceDictionary>